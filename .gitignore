# Local build artifacts
**/.build/
**/.swiftpm/
DerivedData/
.worktrees/

# Heavy binary dependencies prepared locally by scripts
**/Vendor/GhosttyKit.xcframework/

# Local evidence archives. Keep them on disk, but do not commit them.
Backups/

# Packaged apps and DMGs generated locally.
Artifacts/

# macOS filesystem metadata
.DS_Store
**/.DS_Store
